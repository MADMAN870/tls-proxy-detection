# TLS Inspection Proxy — Environment Configuration
# Copy this to .env and fill in your values

# API key for authenticating proxy → API traffic
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
API_KEY=
